import 'package:flutter/material.dart';

class MapTab extends StatelessWidget {
  static const tag = 'map_tab';
  const MapTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('map Tab'),
      ],
    );
  }
}